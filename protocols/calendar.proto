syntax = "proto3";
package cashmere;

enum CalendarType{
  Specified = 0;
  Scripted = 1;
}

message Calendar{
  CalendarType type = 1;
  // {"year"| "month"| "week"}
  string every = 2;
  // {"day": day, "hour": hour, "minute": minute}
  map<string, string> daytime = 3;
}

